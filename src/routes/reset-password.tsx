import { createFileRoute, Link } from "@tanstack/react-router";
import { useState } from "react";
import { supabase } from "@/integrations/supabase/client";
export const Route=createFileRoute('/reset-password')({head:()=>({meta:[{title:'Redefinir senha — Liberato Consulting'},{name:'description',content:'Defina uma nova senha para sua conta.'},{name:'robots',content:'noindex,nofollow'},{property:'og:title',content:'Redefinir senha — Liberato Consulting'},{property:'og:description',content:'Defina uma nova senha para sua conta.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary'}]}),component:ResetPassword});
function ResetPassword(){const [password,setPassword]=useState('');const [message,setMessage]=useState('');const recovery=typeof window!=='undefined'&&window.location.hash.includes('type=recovery');return <div className="mx-auto max-w-md px-6 py-24"><h1 className="text-3xl font-bold">Crie uma nova senha</h1>{recovery?<form className="mt-8" onSubmit={async e=>{e.preventDefault();const {error}=await supabase.auth.updateUser({password});setMessage(error?error.message:'Senha atualizada. Você já pode acessar sua conta.');}}><label className="text-sm font-medium">Nova senha<input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-md border border-input px-3 py-2"/></label><button className="mt-5 rounded-full bg-accent px-6 py-3 font-semibold text-accent-foreground">Salvar senha</button></form>:<p className="mt-5 text-muted-foreground">Abra esta página pelo link recebido no seu e-mail.</p>}{message&&<p className="mt-5 text-sm">{message}</p>}<Link to="/ferramentas" className="mt-6 inline-block text-sm font-semibold text-accent">Voltar à biblioteca</Link></div>}